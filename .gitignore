/target
/dev
/dev_local

# Flatpak build artifacts (created by `just build-flatpak` / `just release`)
/.flatpak-builder/
/flatpak-build/
/flatpak-repo/
/cargo-sources.json

# Flatpak build artifacts (Electron manifest: flatpak-builder + bundle)
/build-dir/
/.flatpak-repo/
/flatpak-node/
*.flatpak
